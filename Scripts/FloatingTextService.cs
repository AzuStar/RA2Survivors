using Godot;

namespace RA2Survivors
{
    public static class FloatingTextService
    {
        public static void SpawnFloatingText(Vector3 position, string text, Color color)
        {
            // FloatingText floatingText = ResourceProvider.LoadEntity<FloatingText>(
            //     EEntityType.FloatingText
            // );
            // floatingText.Text = text;
            // floatingText.Color = color;
            // floatingText.Transform = new Transform3D(Basis.Identity, position);
            // GamemodeLevel1.instance.AddChild(floatingText);
        }
    }
}
